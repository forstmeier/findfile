#!/bin/bash

# removes the infrastructure stack via AWS CloudFormation

STACK_NAME=$( jq -r  '.app.name' <<< "${CONFIG_JSON}" ) 

aws s3 rm s3://$STACK_NAME-storage --recursive

aws cloudformation delete-stack --stack-name $STACK_NAME